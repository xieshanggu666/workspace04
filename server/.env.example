# 数据库（默认按此配置；若 MySQL/Redis 不可用，服务自动降级为 SQLite + 内存缓存）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=zhongxin123
DB_NAME=microgrid

REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# 强制使用离线模式（SQLite + 内存缓存）：设为 1 可跳过连接等待
OFFLINE=0

PORT=3000
JWT_SECRET=campus-microgrid-secret
